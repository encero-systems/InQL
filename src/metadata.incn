"""Library identity and version helpers for IncQL consumers."""


pub def incql_version() -> str:
    """Semantic version of this IncQL package (keep in sync with `incan.toml`)."""
    return "0.1.0"
